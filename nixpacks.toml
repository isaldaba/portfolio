[variables]
NIXPACKS_NO_MIGRATE = "1"

[phases.build]
cmds = [
    "composer install --no-dev --optimize-autoloader",
    "npm ci",
    "npm run build"
]

[start]
cmd = "php artisan config:clear && php artisan key:generate --force && php artisan serve --host=0.0.0.0 --port=$PORT"
